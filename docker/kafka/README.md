This directory is reserved for custom Kafka configuration (optional). The current setup relies on the Bitnami Kafka image with auto topic creation enabled, so no extra configuration is required. Add files here if you need to customise brokers (e.g. ACLs, custom server.properties).

